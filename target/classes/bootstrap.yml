spring:
  application:
    name: collect
  jackson:
    default-property-inclusion: non_null
server:
  port: 9090

logging:
  level:
    root: ERROR
    org.springframework.web: ERROR
    org.hibernate: DEBUG
    com.sscf: DEBUG